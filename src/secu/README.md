현재 맵성성규칙강화 및 ai강화는 결과적으로 80% 정도 초기 의도대로 구현 - ai 사용 0%
아이템 및 횃불 시스템은 초기에 의도치 않게 미리 구현하여 구현 - ai 사용 30%
ui문제는 결과 이전에 해결을 하였으나 기능을 추가하는 과정에성 틀어지는 바람에 결국 ai의 도움을 받으면서 마무리 - ai 사용 80%

리플레이는 결과적으로 초기의도와는 별개로 조금더 효율적인 방안을 찾아서 해결완료 - ai 사용 0%
리플레이의 경우 기존에 MazeGame.java 클래스를 공유하여 재활용하여 귀찮음을 줄이려 하였으나 이미 존재하는 initTimers나 initPlayers의 충돌로
별개 클레스로 나눠서 구현

기존의 로그 개선
- ai 도착 -> [시간초]ai 도착
- 기존보다 로그를 줄이고 필요한 로그를 제외하고 모두 주석처리
- 기존에 존재하던 ReplayScreen 의 로그도 일부 주석처리 - 버튼 생성, 파일 목록 없음은 제외

swing 라이브러리 경고문
- 2025-12-10 기준으로는 뜨지 않던 로그 - 최신 macOS와 특정 JDK를 쓸 때 가끔 뜨는 로그


각 카운트 다운은 activeItem or trap에서 플레이어를 넣고 반환하면 해당하는 플레이어의 카운트다운을 5로 변경하도록 하고 이를 통해 각 플레이어의 카운트 다운을 성립
성능을 위해 anyActive가 false 라면 무조건적으로 Timer off 이런 식으로 타이머를 사용하는 플레이어가 존재하지 않을 떄는 검사만 실행하게 함으로써 렉 최소화
이를 모든 타이머에 적용

1차 과제 -> 2주
2차 과제 한달 and 1주

방향을 넣으니까 자꾸 오류가 남 -> 딕셔너리를 이영해서 1프레임당 그냥 한번 씩 row : col 식으러 하는 방법 보륜
생각보다 벽이나 ai에게 박을 때 return 처리를 해줘도 연타나 여러 요소들이 겹치면서 index를 벗어나는 오류 발생

이 경우에는 그냥 다시 설계하여 짜는게 맞는 듯

지금같은 방식을 계속 쓴다면
or 구분자를 여러개쓰기 = > e 원래대로 전부 구분 row : col이렇게 계속 구분자를 써서 반씩 자르기
그러면 어짜피 개수는 일정하기 때문에 결국 반씩 쪼개면 row 와 col이 나뉘게 됨 -> 해결^^

구분자 결정하기
String parts[] = data.stream.split("e");
=> part[1~6]

3 & 4  = row and col

=> String splits[] = data.stream.split("e");
String rows = splits[홀수]
String cols = splits[짝수]

이러고 위치 데이터 하나씩 불러오기;





